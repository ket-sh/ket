# Advisor implementation plan

> **For agentic workers:** Required sub-skill: use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The gate answers an arrival where it lands: the toolchain look fires mid-session too, and its proposal names the skill route beside the rules route.

**Architecture:** The reply learns its event (`proposal.ts` answers in the shape of the envelope that asked). The proposal text grows the `find-skills` route. The gates bundle arms the same command at PostToolUse. Nothing new decides anything: the seen record keeps naming an arrival once, and the skills carry the method.

**Tech Stack:** The `ket gate toolchain` command, Claude Code hook envelopes, the gates bundle `hooks.json`.

## Global constraints

- Spec: `docs/superpowers/specs/2026-08-04-advisor.md`.
- Work stays on the branch `fix/name-token-injection` and lands through pull request #14, by the owner's call.
- Test-first under probity for `packages/*/src`; observe the red on the new assertion before the write.
- Every commit message follows the caveman-commit skill and ends with the trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Mutation stays at 100 with zero survivors in every touched package.
- Authored markdown passes Vale.

---

### Task 1: The reply learns its event

**Files:**

- Modify: `packages/cli/src/commands/gate/proposal.ts` (the reply carries the event name the envelope brought: `SessionStart` keeps today's shape, `PostToolUse` answers as its own `additionalContext`).
- Modify: `packages/cli/src/commands/gate/toolchain.ts` (read `hook_event_name` from the stdin envelope, default to `SessionStart` when none arrives).
- Tests beside each, red first; mutation zero survivors.

- [ ] Commit: `feat(cli): let the toolchain reply match the event that asked`.

---

### Task 2: The proposal names the skill route

**Files:**

- Modify: `packages/cli/src/commands/gate/proposal.ts` (the asking text names both routes: `mechanical-checks` for the rule, `find-skills` for the skill, each one its own proposal, applied on a yes, and the skill recorded in `skills-lock.json`).
- Tests updated with the behavior, red first.

- [ ] Commit: `feat(cli): propose the skill beside the check`.

---

### Task 3: The gates bundle arms the look

**Files:**

- Modify: `harness/gates/hooks/hooks.json` (two PostToolUse entries running `ket gate toolchain`, timeout 10: matcher `Bash` with `"if": "Bash(bun *)"`, and matcher `Write|Edit`).
- Validate the file parses; the acceptance harness drives one PostToolUse envelope through the binary.
- Modify: `scripts/acceptance-harness.sh` (one proof: a manifest gaining a dependency, a PostToolUse envelope, a reply naming it; a second envelope staying silent).

- [ ] Commit: `feat(harness): look for arrivals where they land`.

---

### Task 4: The chain and the pull request

- [ ] Full repo chain plus mutation on `packages/cli`.
- [ ] Push to pull request #14, retitle it for the pair it now carries, watch its checks, and merge it (squash, delete branch) when they pass, per the owner's call.
