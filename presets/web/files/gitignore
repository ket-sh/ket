node_modules/
.stryker-tmp/
reports/
*.log
.DS_Store
.turbo
dist/
*.tsbuildinfo

# Downloaded by `vale sync`
.vale/styles/Microsoft

# Bun build scratch
*.bun-build

# Built by `vite build`
.output/

# Written by `playwright test`
test-results/
playwright-report/

# Written by `bddgen`
.features-gen/

# Built by `storybook build`
storybook-static/

# Written by the component runner when a test fails
__screenshots__/

# Holds resolved values; varlock reads .env.schema, never this
.env
*.local

# The surface session key lives here while a gate holds; a committed key is a leaked key
.surface.json
